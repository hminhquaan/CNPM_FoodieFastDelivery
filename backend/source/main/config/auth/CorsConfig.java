package config.auth;

// Note: CORS is configured inline in SecurityConfig to avoid bean ambiguity with MVC auto-config.
// This class intentionally declares no @Configuration or @Bean.
public class CorsConfig {
}
